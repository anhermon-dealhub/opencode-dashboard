# Server
HOST=127.0.0.1
PORT=3003

# Where OpenCode stores its session JSON files.
# Default behavior (when unset): $XDG_DATA_HOME/opencode/storage or ~/.local/share/opencode/storage
OPENCODE_STORAGE_PATH=

# OpenCode web UI base URL (used when opening a session)
OPENCODE_WEB_URL=http://localhost:4096

# Dashboard
REFRESH_INTERVAL_MS=5000
SESSION_BUSY_THRESHOLD_MIN=5
SESSION_STALE_THRESHOLD_MIN=30
